---
title: Tangle EVM Client Context
---

import GithubFileReaderDisplay from '/components/GithubFileReaderDisplay';

# Tangle EVM Client Context

The `TangleEvmClientContext` trait provides a standardized interface for interacting with Tangle’s on-chain protocol from your Blueprint via an Alloy-powered EVM client.

## Overview

The context exposes a typed `TangleEvmClient` that supports:

- Transaction submission (service requests, results, registrations)
- State queries (blueprints, services, operator metadata)
- Event/log consumption (typically via producers in the runner)

<GithubFileReaderDisplay
  url="https://github.com/tangle-network/blueprint/blob/v2/crates/contexts/src/tangle_evm.rs"
  fromLine={1}
  toLine={80}
  title="TangleEvmClientContext: construct a typed TangleEvmClient from BlueprintEnvironment"
/>

## Using the Context

### 1. Define Your Context

Derive the context trait on a struct that includes a `BlueprintEnvironment` field:

<GithubFileReaderDisplay
  url="https://github.com/tangle-network/blueprint/blob/v2/crates/macros/context-derive/tests/ui/basic.rs"
  fromLine={1}
  toLine={36}
  title="Deriving TangleEvmClientContext on a custom context"
/>

### 2. Use the Tangle EVM Client

Call `tangle_evm_client()` to get a configured client instance:

<GithubFileReaderDisplay
  url="https://github.com/tangle-network/blueprint/blob/v2/crates/macros/context-derive/tests/ui/basic.rs"
  fromLine={24}
  toLine={40}
  title="Using ctx.tangle_evm_client() in code"
/>
